import { DisplayMathDirective } from './display-math.directive';

describe('DisplayMathDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayMathDirective();
    expect(directive).toBeTruthy();
  });
});
